import React from "react";
import HoverButton from "../../components/UI/HoverButton";

type Props = {};

const index = (props: Props) => {
  return (
    <div>
      <HoverButton text="click here to logout" />
    </div>
  );
};

export default index;
